#include <stdio.h>
#include <string.h>

int main(void) {
char str[50];

printf("Midfix: ");

fgets(str,50,stdin);

int len = strlen(str)-1;

int starting_index = (len - 3)/2;

for(int i=starting_index;i<=starting_index+2;i++)
{
printf("%c",str[i]);
}
printf("\n");
   return 0;

}
