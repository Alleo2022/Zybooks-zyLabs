#include <stdio.h>

int main(void) {

   int n1;
   int n2;
   
   scanf("%d", &n1);
   scanf("%d", &n2);
   
   if (n1 < n2){
      printf("%d ", n1);
   }
      if (n1 > n2){
      printf("Second integer can't be less than the first.");
      }
      
      if (n1 == n2){
         printf("%d ", n1);
      }
   
   while (n1 < n2) {
      n1 += 5;
      if (n1 <= n2) {
      printf("%d ", n1);
      }
   }
   printf("\n");
   }
